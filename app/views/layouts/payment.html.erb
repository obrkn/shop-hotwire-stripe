<!DOCTYPE html>
<html>
  <head>
    <title>ShopHotwireStripe</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "stripe", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <script src="https://js.stripe.com/v3/"></script>
    <%= javascript_include_tag "stripe", "data-turbo-track": "reload", defer: true %>
  </head>
  <body
    data-stripe-publishable-key="<%= Rails.configuration.stripe[:publishable_key] %>"
    data-client-secret="<%= @client_secret %>"
    data-current-user-email="<%= current_user.email %>"
    data-return-url="<%= complete_cart_items_url %>"
    data-turbo="false"
  >
    <%= yield %>
  </body>
</html>
